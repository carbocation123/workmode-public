# data/

## 用途

分离不可修改的仪器原始数据和可再生的处理结果。

## 子目录

- `raw/`：合成 CSV 与明确标记为模拟的 EPR PAR/SPC，只读。
- `processed/`：脚本生成的派生数据、统计摘要、图和 EPR ASC/扫描结果。

## 必须保持的关系

每个 processed 结果必须记录：原始文件、脚本、参数、被排除记录及输出去向。`raw/README.md` 同时登记下游结果，实现双向追溯。
